numbers = [1, 2, 3, 4, 5, 6]

even_numbers = [num      for num in numbers        if num % 2 == 0]

filter_Products=[product  for product in products if product['category']==categroy]

print(even_numbers)